# desk frontend
A react app that utilizes next js for builtin tailwind and google font support.
To install dependencies run 'npm install', and since most of them are in dev mode,
run the app with 'npm run dev'.
